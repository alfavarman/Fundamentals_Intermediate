#intertools product, permutation, combinations, accumulate, groupby, infiniete iterators
##
# from itertools import,
##
# create 2 list 2 elemenst 1 emelent,
#     result = product (l1, l2,) - print
#     result = product (l1, l2, repeat=2) print
##
# Permutation - all possible order of elements in list
# create list with 3 elements
# variable = permutations(listname) , specify length)
# print(list(vairiable)
##
#Combinations and combinations_wtih_replacement
#
##
#Acumulate - list gets new lists that sums elements,
# import operator add value to list name,
# /func=multi multiply the element from list to create new element
# /func=max new list element is highest known.
##
#